# Cloudflare Pages -- redirects
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# Canonical host is penrush.penbag.store. The *.pages.dev preview/default host
# is 301'd to the custom domain so there is one canonical origin (matters for
# the same-origin CSP and for not splitting trust across two hostnames).

https://:project.pages.dev/*   https://penrush.penbag.store/:splat   301

# Friendly alias for the license file.
/license      /LICENSE      301

# NOTE (2026-07-13): the "/terms -> /terms.html" alias was REMOVED. It fought
# Cloudflare's automatic ".html -> extensionless" 308 redirect and produced an
# ERR_TOO_MANY_REDIRECTS loop on the Terms link. CF already serves /terms from
# terms.html via clean-URL handling, so the alias is unnecessary AND harmful.
# Do NOT re-add it. This matches the live-deployed config (direct-upload fix).
